# Copy to `.env` at repo root (gitignored + cursorignored):
#   cp .env.example .env
#
# Existing shell exports take precedence over values here.

# Weights & Biases — https://wandb.ai/authorize
WANDB_API_KEY=
