extends: configs/binary_anchor_ar_patch_decoder_ctx_lb336_hz720_ordinal_norm_uncompressed_bs_mid_vertical_dual.yaml

# Full mid HP vertical_dual campaign with power length schedule g=7.0
# (best mean CRPS on the short g∈{1..10} grid; also best on ETTh1/elec/traffic).

wandb:
  tags: [patch_decoder, ordinal_norm, lb336_hz720, uncompressed, vertical_dual, g7p0, lr_eff_batch_univariate]

experiment:
  name: binary-anchor-ar-patch-decoder-lb336-hz720-ord-unc-bs-mid-vertical-dual-g7p0
  experiment_name: binary_anchor_ar_patch_decoder_ctx_lb336_hz720_ordinal_norm_uncompressed_bs_mid_vertical_dual_g7p0
  binary_length_mode: power
  binary_length_g: 7.0
  binary_length_scale: 1.0
