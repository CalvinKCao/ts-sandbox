# Benchmark control for the cross-attention-only Weather smoke: leave frozen
# tokens live so epoch timing can be compared with the cached configuration.
extends: configs/binary_window_norm_patch_refine_canvas128_p64x6_weather_default_xattn_cache_eval_s16.yaml

experiment:
  name: binary-window-norm-patch-refine-c128-weather-default-xattn-uncached-smoke
  experiment_name: binary_window_norm_patch_refine_c128_weather_default_xattn_uncached_smoke

phases:
  - phase: diffusion_coarse_finetune_hp
    cache_cross_variate_tokens: false
  - phase: diffusion_patch_refine_finetune_hp
    cache_cross_variate_tokens: false
