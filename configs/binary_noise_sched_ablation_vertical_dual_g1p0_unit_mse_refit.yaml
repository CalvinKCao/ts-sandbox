# Refit after unit-rank soft-decode MSE fix: reuse g1p0 synth pretrain + patch
# guidance; retrain vertical_dual finetune only (fresh job stem — do not --resume).
extends: configs/binary_noise_sched_ablation_vertical_dual_g1p0.yaml

wandb:
  tags: [noise_sched_ablation, vertical_dual, uncompressed, g1p0, unit_mse_refit, repr_t1]

experiment:
  name: binary-noise-sched-ablation-vertical-dual-g1p0-unit-mse-refit
  experiment_name: binary_noise_sched_ablation_vertical_dual_g1p0_unit_mse_refit

phases:
  - phase: staged_diffusion_pretrain
    reuse_pretrain_from_config: binary_noise_sched_ablation_vertical_dual_g1p0

  - phase: patch_guidance_finetune_hp
    reuse_checkpoint_from_config: binary_noise_sched_ablation_vertical_dual_g1p0
