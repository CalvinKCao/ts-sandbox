# Canvas128 unique-seg p32x6 pretrain at PatchTST ILI lookback / this-repo overlap.
# Synthetic only. Do not reuse p32x6_pretrain (that donor is lb336/hz96).
# Future canvas width = forecast_length + lookback_overlap = 60+8=68.
extends: configs/binary_window_norm_patch_refine_canvas128_p32x6_pretrain.yaml

geometry:
  lookback_length: 104
  forecast_length: 60
  lookback_overlap: 8
  diffusion_lookback_cap: 104
  past_cond_resize_to_horizon: false
  itrans_lookback_length: 104

wandb:
  tags: [canvas128, p32x6, pretrain, unique_seg, width6, stride5, lb104, hz60, overlap8]

experiment:
  name: binary-window-norm-patch-refine-c128-p32x6-lb104-hz60-pretrain
  experiment_name: binary_window_norm_patch_refine_canvas128_p32x6_lb104_hz60_pretrain
