# g=1.0 baseline seed replicate (seed=44) for CRPS noise-floor estimate.
extends: configs/binary_noise_sched_ablation_elec_unc_g1p0.yaml

wandb:
  tags: [noise_sched_ablation, uncompressed, g1p0, seed_replicate, s44]

experiment:
  name: binary-noise-sched-ablation-elec-unc-g1p0-s44
  experiment_name: binary_noise_sched_ablation_elec_unc_g1p0_s44
  seed: 44
  binary_length_mode: none
  binary_length_g: 1.0
  binary_length_scale: 1.0
