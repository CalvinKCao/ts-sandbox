extends: binary_anchor_stationary_flat_subsets_grad_accum_150_lr_lo.yaml

experiment:
  name: binary-anchor-stationary-flat-subsets-grad-accum-150-lr-lo-max-scale-tune
  experiment_name: binary_anchor_stationary_flat_subsets_grad_accum_150_lr_lo_max_scale_tune

training:
  max_scale_tuning: true
  max_scale_tuning_range: [2.5, 14.0]

phases:
  - phase: diffusion_coarse_finetune_hp
    n_trials: 10
    max_epochs: 20
    patience: 8
    search_space: lr_only
  - phase: diffusion_fine_finetune_hp
    n_trials: 10
    max_epochs: 20
    patience: 8
    search_space: lr_only
  - phase: diffusion_finer_finetune_hp
    n_trials: 10
    max_epochs: 20
    patience: 8
    search_space: lr_only
