# Match the default Weather control exactly, but compute the deterministic
# anchor forward on batches 1, 5, 9, ... of each training epoch.
extends: configs/binary_window_norm_patch_refine_canvas128_p64x6_weather_default_eval_s16_full.yaml

wandb:
  tags: [ablation, weather, default_subset, weather_4v_s2, anchor_every4, canvas128, p64x6, guided, full_eval, stride16]

experiment:
  name: binary-window-norm-patch-refine-c128-weather-default-anchor-every4-eval-s16-full
  experiment_name: binary_window_norm_patch_refine_c128_weather_default_anchor_every4_eval_s16_full
  deterministic_anchor_every_n_batches: 4
