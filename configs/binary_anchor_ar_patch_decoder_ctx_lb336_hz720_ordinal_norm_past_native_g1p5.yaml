# Full past-native stride-2 train+eval with CRPS-calibrated length shift g=1.5.
extends: configs/binary_anchor_ar_patch_decoder_ctx_lb336_hz720_ordinal_norm_past_native.yaml

wandb:
  tags: [patch_decoder, ordinal_norm, lb336_hz720, past_native, crps_g1p5]

experiment:
  name: binary-anchor-ar-patch-decoder-lb336-hz720-past-native-g1p5
  experiment_name: binary_anchor_ar_patch_decoder_ctx_lb336_hz720_ordinal_norm_past_native_g1p5
  binary_length_mode: power
  binary_length_g: 1.5
  binary_length_scale: 1.0

phases:
  - phase: staged_diffusion_pretrain
    reuse_pretrain_from_config: binary_anchor_ar_patch_decoder_ctx_lb336_hz720_fixed

  - phase: patch_guidance_finetune_hp
    reuse_checkpoint_from_config: binary_anchor_ar_patch_decoder_ctx_lb336_hz720_ordinal_norm

  - phase: diffusion_coarse_finetune_hp
    fixed_tuned_params:
      learning_rate: 2.41e-4
      ema_decay: 0.995
      prediction_target: x0
      loss_weighting: min_snr
      min_snr_gamma: 2.0
      binary_noise_schedule: linear
      binary_length_mode: power
      binary_length_g: 1.5
      binary_length_scale: 1.0

  - phase: diffusion_fine_finetune_hp
    fixed_tuned_params:
      learning_rate: 2.41e-4
      ema_decay: 0.995
      prediction_target: x0
      loss_weighting: min_snr
      min_snr_gamma: 2.0
      binary_noise_schedule: linear
      binary_length_mode: power
      binary_length_g: 1.5
      binary_length_scale: 1.0
