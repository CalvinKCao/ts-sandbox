extends: binary_anchor_stationary_flat_subsets_grad_accum_150_fourier_freq.yaml

experiment:
  name: binary-anchor-stationary-flat-subsets-grad-accum-150-fourier-freq-diag
  experiment_name: binary_anchor_stationary_flat_subsets_grad_accum_150_fourier_freq_diag

phases:
  - phase: diffusion_fine_finetune_hp
    n_trials: 1
    max_epochs: 12
    patience: 8
    search_space: lr_only

  - phase: staged_eval
    tune_sampler: false
    n_samples: 1
    probabilistic_n_samples: 1
    probabilistic_sampler: dpmpp
    probabilistic_num_inference_steps: 5
    batch_size: 2
    gmm_components: 1
    topk_max: 1
    skip_eval_visualizations: true
