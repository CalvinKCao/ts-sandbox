# Full Jul 6-8 ordinal_norm geometry + CRPS-tuned g=1.5 (electricity winner, stride2_resize ablation).
extends: configs/binary_anchor_ar_patch_decoder_ctx_lb336_hz720_ordinal_norm.yaml

wandb:
  tags: [patch_decoder, ordinal_norm, lb336_hz720, crps_g1p5, resize_horizon]

experiment:
  name: binary-anchor-ar-patch-decoder-lb336-hz720-ordinal-norm-g1p5
  experiment_name: binary_anchor_ar_patch_decoder_ctx_lb336_hz720_ordinal_norm_g1p5
  binary_length_mode: power
  binary_length_g: 1.5
  binary_length_scale: 1.0

phases:
  - phase: diffusion_coarse_finetune_hp
    fixed_tuned_params:
      learning_rate: 2.41e-4
      ema_decay: 0.995
      prediction_target: x0
      loss_weighting: min_snr
      min_snr_gamma: 2.0
      binary_noise_schedule: linear
      binary_length_mode: power
      binary_length_g: 1.5
      binary_length_scale: 1.0

  - phase: diffusion_fine_finetune_hp
    fixed_tuned_params:
      learning_rate: 2.41e-4
      ema_decay: 0.995
      prediction_target: x0
      loss_weighting: min_snr
      min_snr_gamma: 2.0
      binary_noise_schedule: linear
      binary_length_mode: power
      binary_length_g: 1.5
      binary_length_scale: 1.0
