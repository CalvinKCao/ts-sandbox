# Traffic 862-variate: inductor compile OOMs at batch=1. Keep compile off.
# Coarse bs=1 still OOMs on L40S without activation checkpointing.
extends: configs/binary_ordinal_patch_refine_canvas128_p64x6_allv_randwin_lr10.yaml

training:
  torch_compile: false
  use_gradient_checkpointing: true

wandb:
  tags: [all_variates, randwin, lr10, canvas128, p64x6, ordinal, win_frac_1, patch_frac_1, lr_batch_ema, anchor_every4, eval_s1_n10_steps4, nopruner, from_pretrain, no_compile]
