# Confirmation / seed-floor replicate (seed=43) at g1p5.
extends: configs/binary_noise_sched_ablation_past_native_g1p5.yaml

wandb:
  tags: [noise_sched_ablation, past_native, g1p5, seed_replicate, s43]

experiment:
  name: binary-noise-sched-ablation-past-native-g1p5-s43
  experiment_name: binary_noise_sched_ablation_past_native_g1p5_s43
  seed: 43
