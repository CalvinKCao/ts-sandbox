extends: binary_anchor_stationary_flat_subsets_ema_sweep_base.yaml

# Same synthetic pretrain + iTrans reuse as EMA sweep; only diffusion finetune/eval re-run.
# Leaf configs set training.diffusion_effective_batch_multiplier.
