# Eval-only canvas128 electricity: stride 1, 66% windows, 10 MC samples.
# Weights stay in the original job dir. Resume:
# results/eval_resume/canvas128_s1_n10_f66/
# Absolute ckpt path so this leaf works from a pre-xattn worktree.
extends: configs/binary_window_norm_patch_refine_canvas128_p64x6_electricity.yaml
replace_phases: true

wandb:
  tags: [evaluation_only, canvas128, stride1, n10, f66, electricity]

experiment:
  name: binary-window-norm-patch-refine-c128-electricity-eval-s1-n10-f66
  experiment_name: binary_window_norm_patch_refine_canvas128_p64x6_electricity_eval_s1_n10_f66
  eval_source_checkpoint_dir: /scratch/ccao87/ts-sandbox/results/ckpts/08-04-4597054-electricity-binary_window_norm_patch_refine_canvas128_p64x6_electricity

visualization:
  viz_binary_mmpd_gap: false
  viz_binary_mmpd_redbox: false

phases:
  - phase: staged_eval
    anchor_only: false
    eval_test_fraction: 0.66
    test_stride: 1
    eval_progress_key: canvas128_s1_n10_f66
    n_samples: 1
    probabilistic_n_samples: 10
    tune_sampler: false
    probabilistic_sampler: quad_t
    probabilistic_num_inference_steps: 20
    batch_size: 8
    gmm_components: 10
    topk_max: 3
    skip_eval_visualizations: true
    probe_eval_batch_size: true
