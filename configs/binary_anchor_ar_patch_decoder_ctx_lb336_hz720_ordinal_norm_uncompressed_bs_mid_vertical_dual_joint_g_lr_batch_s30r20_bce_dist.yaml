# Ablation vs joint_g_lr_batch_s30r20: no soft-decode 1D MSE in the anchor term.
# Anchor = distance-weighted map BCE only (W=1+α|r−k| on CDF pixels).
# Regular diffusion BCE already uses the same distance weights (inherited).
# All Optuna / refit / reuse / aug / eval knobs identical to the joint leaf.
extends: configs/binary_anchor_ar_patch_decoder_ctx_lb336_hz720_ordinal_norm_uncompressed_bs_mid_vertical_dual_joint_g_lr_batch_s30r20.yaml

wandb:
  tags:
    - patch_decoder
    - ordinal_norm
    - lb336_hz720
    - uncompressed
    - vertical_dual
    - joint_g_lr_batch
    - reuse_g1_pretrain
    - search16_refit20
    - train_window_aug
    - anchor_mse_objective
    - anchor_bce_dist_no_soft_mse
    - repr_t1

experiment:
  name: binary-anchor-ar-patch-decoder-lb336-hz720-ord-unc-bs-mid-vd-joint-g-lr-batch-s30r20-bce-dist
  experiment_name: binary_anchor_ar_patch_decoder_ctx_lb336_hz720_ordinal_norm_uncompressed_bs_mid_vertical_dual_joint_g_lr_batch_s30r20_bce_dist
  # λ=1 → anchor is map BCE only; (1-λ)=0 drops soft-decode 1D MSE.
  anchor_mse_proxy_lambda: 1.0
  # Explicit (same as vertical_dual parent): distance-weighted BCE W=1+α|r−k|.
  binary_use_boundary_weighted_bce: true
  binary_cdf_distance_alpha: 1.0
