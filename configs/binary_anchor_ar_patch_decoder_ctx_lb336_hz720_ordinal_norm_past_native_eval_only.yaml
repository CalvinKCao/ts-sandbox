# Re-eval Jul-08 ordinal_norm ckpts with past_native eval geometry (past_cond_resize=false).
# Use: --resume --ckpt-config binary_anchor_ar_patch_decoder_ctx_lb336_hz720_ordinal_norm
extends: configs/binary_anchor_ar_patch_decoder_ctx_lb336_hz720_ordinal_norm_past_native.yaml

experiment:
  name: binary-anchor-ar-patch-decoder-lb336-hz720-ordinal-norm-past-native-eval-only
  experiment_name: binary_anchor_ar_patch_decoder_ctx_lb336_hz720_ordinal_norm_past_native_eval_only

phases:
  - phase: staged_eval
    eval_test_fraction: 1.0
    test_stride: 4
    n_samples: 1
    probabilistic_n_samples: 20
    tune_sampler: false
    probabilistic_sampler: dpmpp
    probabilistic_num_inference_steps: 20
    batch_size: 8
    gmm_components: 10
    topk_max: 3
    skip_eval_visualizations: true
