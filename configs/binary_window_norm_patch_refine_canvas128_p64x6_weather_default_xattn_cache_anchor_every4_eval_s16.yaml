# Same xattn-cache Weather 4v architecture as job 4806439 (cross-attn + frozen
# decoder tokens, no 2D guidance channel). Deterministic-anchor forward on
# batches 1, 5, 9, ... . Reuse 4806439's synthetic pretrain; fail if missing.
extends: configs/binary_window_norm_patch_refine_canvas128_p64x6_weather_default_xattn_cache_eval_s16.yaml

wandb:
  tags: [ablation, weather, default_subset, cross_attention_only, frozen_token_cache, xattn_cache, anchor_every4, compile, reuse_pretrain_4806439, canvas128, p64x6, full_eval, stride16]

experiment:
  name: binary-window-norm-patch-refine-c128-weather-xattn-cache-anchor-every4-eval-s16
  experiment_name: binary_window_norm_patch_refine_c128_weather_default_xattn_cache_anchor_every4_eval_s16
  deterministic_anchor_every_n_batches: 4

phases:
  - phase: staged_diffusion_pretrain
    reuse_pretrain_from_config: binary_window_norm_patch_refine_canvas128_p64x6_weather_default_xattn_cache_anchor_every8_eval_s16
    require_reuse_pretrain: true
    shared_cache: false
