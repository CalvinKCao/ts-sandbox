extends: binary_anchor_ar_grad_accum_base.yaml

training:
  diffusion_effective_batch_multiplier: 8.0

experiment:
  name: binary-anchor-ar-grad-accum-800
  experiment_name: binary_anchor_ar_grad_accum_800
