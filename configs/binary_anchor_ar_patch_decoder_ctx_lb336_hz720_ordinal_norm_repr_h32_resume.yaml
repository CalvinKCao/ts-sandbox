# Resume overlay for repr_h32 after disk-quota crashes.
#
# Prefer resuming the *original* config (same stem) after the should_skip fix:
#   ./test_submit.sh --resume \
#     --configs configs/binary_anchor_ar_patch_decoder_ctx_lb336_hz720_ordinal_norm_repr_h32.yaml \
#     --datasets ETTh1,weather,exchange_rate --gpu l40s --time 6:00:00 \
#     --wandb-project ts-sandbox-leaderboard
#
# If you use this overlay file instead, pass --ckpt-config so the stem matches:
#   ./test_submit.sh --resume \
#     --configs configs/binary_anchor_ar_patch_decoder_ctx_lb336_hz720_ordinal_norm_repr_h32_resume.yaml \
#     --ckpt-config binary_anchor_ar_patch_decoder_ctx_lb336_hz720_ordinal_norm_repr_h32 \
#     --datasets ETTh1,weather,exchange_rate --gpu l40s --time 6:00:00 \
#     --wandb-project ts-sandbox-leaderboard
#
# Cached best.pt+metadata are reused; missing/corrupt fine ckpts retrain fine only.

extends: configs/binary_anchor_ar_patch_decoder_ctx_lb336_hz720_ordinal_norm_repr_h32.yaml

phases:
  - phase: staged_diffusion_pretrain
    phase1_config_name: binary_dual_scale_staged
    reuse_pretrain_from_config: null
    shared_cache: false
    retrain_synthetic_itrans: true

  - phase: patch_guidance_finetune_hp
    reuse_checkpoint_from_config: binary_anchor_ar_patch_decoder_ctx_lb336_hz720_ordinal_norm

  - phase: diffusion_coarse_finetune_hp
    reuse_tuned_params_from: null
    retrain: false
    n_trials: 1
    max_epochs: 20
    patience: 8

  - phase: diffusion_fine_finetune_hp
    reuse_tuned_params_from: null
    retrain: false
    n_trials: 1
    max_epochs: 20
    patience: 8
