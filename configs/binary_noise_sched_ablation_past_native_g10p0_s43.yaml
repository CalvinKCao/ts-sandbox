# Confirmation / seed-floor replicate (seed=43) at g10p0.
extends: configs/binary_noise_sched_ablation_past_native_g10p0.yaml

wandb:
  tags: [noise_sched_ablation, past_native, g10p0, seed_replicate, s43]

experiment:
  name: binary-noise-sched-ablation-past-native-g10p0-s43
  experiment_name: binary_noise_sched_ablation_past_native_g10p0_s43
  seed: 43
