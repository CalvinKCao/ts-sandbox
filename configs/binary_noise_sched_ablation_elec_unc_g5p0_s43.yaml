# Confirmation seed (seed=43) at g5p0 — sanity check vs seed=42 at final recommended g.
extends: configs/binary_noise_sched_ablation_elec_unc_g5p0.yaml

wandb:
  tags: [noise_sched_ablation, uncompressed, g5p0, confirm_seed, s43]

experiment:
  name: binary-noise-sched-ablation-elec-unc-g5p0-s43
  experiment_name: binary_noise_sched_ablation_elec_unc_g5p0_s43
  seed: 43
